---
import { twMerge } from "tailwind-merge";

interface Props {
    classes?: string;
}

const { classes } = Astro.props;

const classList = twMerge("stack", classes);
---

<div class={classList}><slot /></div>
